# Legacy site archive (NetSci-X Auckland) — kept locally, not pushed
legacy-site.zip

# Private conference materials (SNSF proposal, agenda) — context only, do not publish
conference/

# Headshot build scratch (raw source photos + processing script)
_src_photos/

# Raw high-res source portraits (processed into images/headshots/, not served)
images/headshot_cropped.png
images/PHOTO-2025-10-15-23-00-33.jpg

# Claude Code / local tooling (preview config, local settings)
.claude/

# macOS cruft
.DS_Store
**/.DS_Store
